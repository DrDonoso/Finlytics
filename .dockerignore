# Version control
.git
.gitattributes
.gitignore

# Secrets — never in image
.env
.env.*
!.env.example
# Exception: frontend/.env.demo only sets VITE_DEMO=1 (no secret) and is REQUIRED
# by the Dockerfile's `demo` target — `vite build --mode demo` reads it. Without
# this rule the demo image would silently ship the production SPA, i.e. no mock
# API at all.
!frontend/.env.demo

# Python artifacts
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/

# Frontend: node_modules and compiled output (rebuilt inside the image)
frontend/node_modules/
frontend/dist/
frontend/dist-demo/
frontend/.vite/

# Tests (not needed at runtime)
tests/

# Documentation assets (README screenshots — never used at runtime)
docs/

# Project management / squad docs
.squad/

# Editor & OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
