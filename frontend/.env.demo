# Demo build flag, loaded by Vite for `--mode demo` (see package.json scripts).
# Serves the app entirely from the in-browser synthetic dataset via MSW:
# no backend, no database, no real data. Contains no secrets — safe to commit.
VITE_DEMO=1
