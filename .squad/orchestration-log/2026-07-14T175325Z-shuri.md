# Shuri — Backend Integration: Indexa Refinement & Data Redesign

**Date:** 2026-07-14T17:53:25Z  
**Agent:** Shuri (Backend)  
**Context:** Phase 2 — Indexa Capital Connector continuation (post 4a7673c)

---

## Work Completed

1. **Bug A: `total_value` Fixed (Fallback Chain)**
   - Issue: `total_value` was always 0 (reading from top-level `data["total_amount"]` which is absent on real accounts)
   - Fix: Implemented 4-step fallback chain using `portfolios[0].total_amount` as authoritative source
   - Live account test: €20,559.52 correctly derived from `portfolios[0]` snapshot
   - Real value: cash €67.16 + instruments €20,492.36

2. **Bug B: Holdings Deduplication by ISIN**
   - Issue: Indexa `/fiscal-results` returns multiple entries per subscription lot; same fund appeared as multiple rows
   - Fix: `get_portfolio` now groups by `instrument.identifier` (ISIN) or `instrument.name`
   - Aggregation: sum `current_value`, `cost_basis`, `units`; recompute `gain_loss_pct` across group
   - Result: One holding per fund (matches Indexa's own fiscal view)

3. **Enhancement: Extended Return Fields**
   - Added 6 new fields to `InvestmentReturns` schema
   - Multi-account aggregation rules documented
   - Tests: 9 new test cases for bugs + fields

4. **Backend Response Redesigned**
   - `GET /api/investments/portfolio` extended with:
     - `value_series` — 709 daily points, YYYY-MM-DD dates
     - `contributions_series` — daily cumulative net contributions (716 points)
     - `monthly_returns` — month × year matrix
     - `drawdown` — max drawdown object
     - `cash_invested` — cash vs. instruments breakdown

5. **Live Data Verification**
   - Probed owner's real Indexa account
   - Confirmed all data shapes present and reconciled correctly

---

## Code Changes

- `src/finlytics/investments/base.py` — `NormalizedReturns` (6 new fields)
- `src/finlytics/investments/indexa.py` — `_fetch_performance`, `get_portfolio`
- `src/finlytics/api/schemas.py` — `InvestmentReturns` (6 new fields)
- `src/finlytics/investments/service.py` — `_aggregate` (multi-account rules)
- `tests/api/test_investments.py` — 9 new test cases

**Test status:** 921 passed, 2 skipped

---

## Handoff

Data ready for Vision (Frontend). All series, matrix, and drawdown metrics available.
