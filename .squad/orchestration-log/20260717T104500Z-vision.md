# Vision — Import preview duplicate override UI

**Timestamp:** 2026-07-17T10:45:00Z  
**Scope:** Frontend import confirmation and duplicate override control.

## Summary

Added per-row duplicate override control in ImportPreviewTable. Clicking "No es duplicada / Not a duplicate" beside the duplicate badge sets `allow_duplicate=true` on the preview row, clears the duplicate UI state immediately, and excludes it from duplicate counts and flagged-only filtering. Override survives re-checks because the UI state is persistent in the preview component. Confirm payload sends `allow_duplicate` per transaction.

## Validation

- `cd frontend && npm run build` passes with zero TypeScript errors
- i18n updated: `Dict` interface, ES/EN implementations
- New `allow_duplicate` field in `ConfirmIn` types
- Vite chunk-size warning (pre-existing) unchanged

## Files touched

- `frontend/src/components/ImportPreviewTable.tsx` (override control, state management)
- `frontend/src/types.ts` (ConfirmIn schema update)
- `frontend/src/i18n/index.ts`, `es.ts`, `en.ts` (bilingual strings)
