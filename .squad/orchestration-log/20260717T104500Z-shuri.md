# Shuri — Force-import duplicate transactions

**Timestamp:** 2026-07-17T10:45:00Z  
**Scope:** Backend import confirmation and idempotent persistence.

## Summary

Implemented backend force-import override for duplicates. Added `allow_duplicate: bool = False` to `ExtractedTransaction` schema. Extended `compute_dedup_hash()` with optional `disambiguator` parameter to generate unique hashes for override rows. Modified `upsert_transactions()` to hash a `uuid4()` disambiguator only when `allow_duplicate=True`, allowing overridden transactions to INSERT instead of being skipped by ON CONFLICT.

## Validation

- `pytest` full suite: 1160 passed, 2 skipped
- `/check-duplicates` unchanged; override behavior isolated to confirm path
- Schema updated; no breaking changes to existing imports

## Files touched

- `src/finlytics/models/transaction.py` (ExtractedTransaction, compute_dedup_hash, upsert_transactions)
- `src/finlytics/api/imports.py` (ConfirmIn, /confirm route)
- Tests updated and passing
