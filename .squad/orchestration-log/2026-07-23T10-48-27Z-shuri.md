# Orchestration Log: Shuri — Cache Schema Versioning

**Timestamp:** 2026-07-23T10:48:27Z  
**Agent:** Shuri (Backend Engineer)  
**Task:** Portfolio cache schema versioning — prevent stale post-deploy JSON

## Scope

Implement `_PORTFOLIO_SCHEMA_VERSION` in service.py to auto-invalidate cache rows written by pre-deploy code (missing `contribution_events` field). No migration; self-heals on first request post-deploy.

## Implementation Summary

**File:** `src/finlytics/investments/service.py`

- Added `_PORTFOLIO_SCHEMA_VERSION = 2` constant
- Modified `_serialize_portfolio()` to embed `_schema_version` in all new cache rows
- Modified `_get_db_cache()` to detect version mismatch: delete row + flush + return None → MISS synchronous refetch
- `_deserialize_portfolio()` ignores the key (unchanged)

**Test coverage:** 9 versioning tests in `tests/api/test_investments.py`

## Verification

- Suite: 1366 passed, 2 skipped, 0 failed ✅
- Docker: clean startup, no migration needed ✅
- Frontend: no changes ✅

## Status

✅ IMPLEMENTED — Ready for integration
