# Vision — Import Quality UI Slice 1

**Timestamp:** 2026-07-16T13:00:45Z  
**Agent:** vision  
**Status:** DONE

## Summary

Frontend advisory quality panel per preview file/account block showing signal counts and expandable rows. Includes flagged-only row filter, cell badges on flagged fields, and bilingual EN/ES localization by code.

## Artifacts

- Types: `frontend/src/api/types.ts`
- Mock: `frontend/src/api/mock.ts`
- i18n: `frontend/src/i18n/{index,es,en}.ts`
- UI: `frontend/src/components/{ImportModal,ImportPreviewTable}.tsx`

## Features

1. **Placement:** Compact quality panel inside each preview file/account accordion, before table
2. **Duplicates:** Merged intra_batch_duplicate rows with existing check-duplicates flow
3. **Filter:** Solo marcadas / Flagged only toggle with badged cells per signal
4. **Non-blocking:** Confirm/import enabled regardless of warnings/errors
5. **Mock:** Full representative preview quality data for VITE_USE_MOCK=1

## Validation

`cd frontend && npm run build` — zero TypeScript errors, expected chunk warning ✓

## Design

Advisory only. Backend codes never rendered. Counts per file/account. Rows badged but not auto-deselected.
