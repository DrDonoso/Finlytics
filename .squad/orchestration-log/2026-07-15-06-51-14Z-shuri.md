# Orchestration Log — Shuri

**Agent:** Shuri (Backend Engineer)  
**Session:** Fidelity ESPP Statement-Import Feasibility Probe  
**Timestamp:** 2026-07-15T06:51:14Z  
**Status:** Complete

## Contribution

- Evaluated backend feasibility: price sourcing, DB persistence, provider ABC integration
- Recommended **Stooq** (primary, HTTP GET CSV, no auth) + **yfinance** (fallback) for daily MSFT price + EUR/USD FX — no encrypted token needed (unlike Indexa)
- Designed two new DB tables:
  - `espp_lots`: tax-lot style immutable records (purchase_date, shares, price_per_share_usd, cost_basis_usd, dedup_hash for idempotence)
  - `price_cache`: ticker, price_date, close_price_usd, fx_eur_usd, close_price_eur, source
- Recommended **on-request fetch + DB cache** strategy over background scheduler (simpler, survives Docker restarts, one HTTP call/day/ticker)
- Proposed `investment_connections.token_enc` → NULLABLE via Alembic migration 0014 (Indexa stays NOT NULL at app layer; Fidelity ESPP = NULL)
- Designed Provider ABC extensions: `provider_type` attribute, `import_statement()` method, optional `refresh_price_cache()` hook
- Proposed minimal `service.py` change: registry dict (plugin_id → provider_instance); branch by provider_type

## Deliverable

Decision memo: `.squad/decisions/inbox/shuri-fidelity-holdings-backend.md` → merged into `.squad/decisions.md`  
Skill doc: `.squad/skills/market-data-cache/SKILL.md` (created, reusable for price caching patterns)

## Implementation Notes

- **Idempotence:** SHA-256 of natural key `(connection_id, purchase_date, shares, purchase_price_usd)` → INSERT ... ON CONFLICT DO NOTHING
- **Number normalization:** Handle European comma-decimal format before hashing: `float(str(value).replace(",", "."))`
- **Migration design** (not yet written): token_enc NULLABLE, espp_lots table, price_cache table
- **No new dependencies** for price source (Stooq is HTTP GET; yfinance added to pyproject.toml if needed)
- **Valuation query:** SUM(lots.shares) × close_price_eur from price_cache, with graceful degradation if price fetch fails
