# Vision — Frontend Implementation: Complete Investments Redesign

**Date:** 2026-07-14T17:53:25Z  
**Agent:** Vision (Frontend Engineer)  
**Context:** Phase 2 — Indexa Capital Connector UI implementation (post 4a7673c)

---

## Work Completed

1. **Page Layout Rebuild**
   - Refactored `InvestmentsPage.tsx` to new grid structure:
     - Removed 5 KPI cards + `inv-charts-row`
     - Added `inv-top-row` (1fr/1fr) with summary card + donut grid
     - Moved returns matrix into left column (via `inv-left-col` wrapper)
     - Full-width evolution chart with period selector + EUR/% toggle

2. **Evolution Chart Implementation**
   - Fixed blank-chart bug: `ResponsiveContainer height='100%'` collapse → numeric height (300px)
   - Added period selector (dropdown or buttons: 1W, 1M, 1Y, all)
   - Implemented EUR/% toggle for value display
   - Enhanced hover tooltip: shows dd/mm/yyyy format (locale-aware)
   - Auto-domain Y-axis (scales to data range, not hardcoded)

3. **Holdings Table Enhancement**
   - Added sortable columns (click header to sort)
   - New `units` column (number of fund shares)
   - Retenciones sign fix: display as negative (negated on render)
   - Localized percentage display (2 decimal places)

4. **Donut Visualizations**
   - Two-donut grid layout (asset class + instrument)
   - Per-instrument donut with compact legend (8×8px color swatches)
   - Center label fix (position: absolute; inset: 0 now correctly centered at 220px height)
   - 12-color instrument palette (`INSTRUMENT_PALETTE` const)

5. **Info Tooltips**
   - Tooltip infrastructure: portal to `document.body` with fully-inline box styles
   - Escaped overflow clipping + CSS cascade via inline styles
   - Tooltips on metrics headers (TWR, MWR, Volatility) with explanatory text
   - Tooltips on returns table headers (G/P, % labels)
   - Keyboard accessible (Tab → :focus-visible shows tooltip)

6. **Returns Matrix & Metrics**
   - Monthly returns matrix: 3 years × 12 months grid
   - Matrix in left column (scrolls horizontally if needed in narrow view)
   - Returns metrics strip: 3 cells (TWR / MWR / Volatility)
   - Returns table: 7 rows (week/month/year returns, TWR total/annual, XIRR, volatility)
   - Color coding: green +, red −, neutral for volatility

---

## Code Implementation

- `frontend/src/pages/InvestmentsPage.tsx` — full redesign + all features
- `frontend/src/components/InvestmentCharts.tsx` — evolution chart (new)
- `frontend/src/components/InvestmentMetrics.tsx` — metrics strip (new)
- `frontend/src/components/ReturnsMatrix.tsx` — monthly matrix (new)
- `frontend/src/investmentColors.ts` — palette + constants

---

## Testing & Verification

- Tested with live portfolio data (owner's account)
- Evolution chart renders 709 data points correctly
- Holdings table deduplicates by ISIN and sorts
- Responsive layouts verified at 901px, 600px breakpoints
- Tooltip portal prevents overflow clipping

---

## Status

✓ Full page redesign shipped in commit 955000d (pushed to main)
