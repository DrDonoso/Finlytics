# Orchestration Log — Banner

**Agent:** Banner (Data/AI Engineer)  
**Session:** Fidelity ESPP Statement-Import Feasibility Probe  
**Timestamp:** 2026-07-15T06:51:14Z  
**Status:** Complete

## Contribution

- Conducted empirical PDF extraction probe against owner's real Fidelity ESPP statement (PII redacted per security policy)
- Confirmed statement is fully parseable: text layer real (not scanned), stable Fidelity machine-generated template
- Mapped all target fields (ticker, cumulative shares, period-end price, cost basis, per-lot purchase date/price/quantity, ESPP plan metadata) — all present and reliably locatable
- Recommended **hybrid approach** (structured parse + LLM) over pure regex for robustness; cost negligible (~$0.01–0.05/PDF for ~4 imports/year)
- Designed Pydantic schemas: `ESPPLot` (purchase record) + `ESPPHoldingSnapshot` (period snapshot with lots list)
- Identified and documented edge cases: fractional shares (≥3 decimals confirmed), multiple lots per purchase, accumulativity, dividends, PII redaction requirements
- Effort estimate: ~2.75 days (models + prompts + extractor + PII expansion + tests)

## Deliverable

Decision memo: `.squad/decisions/inbox/banner-fidelity-espp-extraction.md` → merged into `.squad/decisions.md`  
Skill doc: `.squad/skills/brokerage-statement-parsing/SKILL.md` (created, reusable for future statement-based brokers)

## Flags for Implementation

- **Privacy:** Full name, postal address, participant number (`I` + 8 digits) require redaction before LLM calls
- **PDF never stored:** Only structured extracted data persisted to DB
- **Hybrid schema:** ESPPLot + ESPPHoldingSnapshot to be created in `src/finlytics/extraction/espp_schema.py`
