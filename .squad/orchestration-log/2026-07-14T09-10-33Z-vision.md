# Orchestration Log — Vision

**Date:** 2026-07-14T09:10:33Z  
**Agent:** Vision (Frontend Engineer)  
**Phase:** 2 — Indexa Capital Connector

## Responsibility

Frontend implementation: wizard component, ConnectorsPage state management, InvestmentsPage populated viz.

## Deliverables Completed

1. **API Types & Client**
   - frontend/src/api/types.ts: InvestmentReturns, ValuePoint, CashInvestedSplit, InvestmentHolding, InvestmentPortfolio, InvestmentConnection, ValidatedAccount, ValidateAccountsResponse
   - frontend/src/api/client.ts: 5 new functions (validateIndexaToken, connectPlugin, getConnections, disconnectConnection, getInvestmentPortfolio)
   - frontend/src/api/mock.ts: Full mock implementations with _mockConnected flag for demo mode

2. **Wizard Component**
   - frontend/src/components/IndexaWizard.tsx: 4-step modal per Wanda's spec
   - Step 1: Intro (logo, desc, token page link, security note)
   - Step 2: Token input (password field, validate button disabled until non-empty)
   - Step 3a: Validating (spinner)
   - Step 3b: Error banner (invalid token, back to Step 2)
   - Step 3c: Account selection (checkboxes, all pre-selected)
   - Step 4: Success (connected accounts, navigate to Investments)

3. **ConnectorsPage Rewritten**
   - Fetches plugins + connections in parallel
   - Indexa card renders in 3 states: available (connect button), connected (badge + disconnect), error (badge + retry)
   - Other cards remain "coming soon"
   - Disconnect triggers confirmation then DELETE call

4. **InvestmentsPage Rewritten**
   - Header + connected account strip
   - KPI row (5 cards: total value, invested, P&L, TWR, XIRR)
   - Charts row: value-over-time AreaChart + allocation donut with table
   - Holdings table: sortable by name/value/P&L
   - States: loading, error, empty (no connections), populated
   - Formats: currency EUR, gain_loss_pct × 100, date "YYYYMMDD" → "MMM YY"

5. **i18n Integration**
   - 44 new keys added to Dict interface, es.ts, en.ts
   - Wizard: 24 keys
   - Investments populated: 15 keys + 1 function (invAccountUpdated)
   - Connector cards: 4 keys

## Status

SHIPPED. `npm run build` 0 TS errors. Full flow testable in demo mode (VITE_USE_MOCK=1).

## Notes

Decision: Union type for WizardStep with string literals (avoids boolean flag bloat). Parallel data fetch on InvestmentsPage. Mock includes full connect→view flow.
