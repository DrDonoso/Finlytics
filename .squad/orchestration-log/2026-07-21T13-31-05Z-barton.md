# Orchestration Log — Barton (QA / Tests)

**Timestamp:** 2026-07-21T13:31:05Z  
**Agent:** Barton (Tester / QA Engineer)  
**Slice:** Import-time opening balance  
**Status:** ✅ COMPLETED  

---

## Assigned Tasks

1. Write 11 edge-case tests in test_import_opening_balance.py
2. Cover: new account IBAN, new account name, existing account ignored, zero/null balance, negative (overdraft), date inference, DB dedup, idempotence, empty transactions, response contract
3. Verify all tests pass against Shuri's implementation
4. Coordinate with Shuri on patch paths (helper location)

---

## Test Suite: 11 Edge Cases

| ID | Test | Coverage |
|----|------|----------|
| TC-1 | New account by IBAN + opening_balance > 0 | ImportRun "saldo-inicial" created, period correct |
| TC-2 | New account by NAME + opening_balance > 0 | ImportRun "saldo-inicial" created |
| TC-3 | Existing account + opening_balance | Ignored silently (was_created=False) |
| TC-4 | opening_balance = 0 | No synthetic transaction created |
| TC-5 | opening_balance = null | No synthetic transaction created |
| TC-6 | Negative opening_balance | Overdraft handled, no crash |
| TC-7 | Date inference from out-of-order txs | opening_date = min(txs) − 1 day, correct period |
| TC-8a | DB-level dedup (ON CONFLICT) | Duplicate INSERT absorbed, idempotent |
| TC-8b | Idempotence: 2nd confirm on same extract | was_created=False on 2nd call, no helper call |
| TC-9 | Empty transactions + opening_balance | Guard prevents crash (min([]) ValueError) |
| TC-10 | Response contract validation | ImportResult structure valid, all fields present |

---

## Verification

- All 11 tests: ✅ PASS against Shuri's implementation
- Full suite scope (import/account/confirm): 172 passed, 0 failed
- Full suite overall: 1275 passed, 2 skipped, 0 failed
- No regressions

---

## Technical Decisions

**Patch paths:** Double-patch strategy confirmed for tests inspecting opening_date ImportRun:
- Patch `finlytics.api.imports.ImportRun` for main import run (id=42)
- Patch `finlytics.db.repository.ImportRun` for opening balance synthetic run
- Use `side_effect=_make_ir_side_effect(...)` to dispatch by source_filename

**Empty transaction edge case:** TC-9 critical — prevents 500 error if frontend allows empty txs + opening_balance payload.

---

## Issues & Decisions

None. All guards verified. Idempotence double-checked (call-level + DB-level). No crash scenarios.

---

## Sign-off

Barton: ✅ QA passed. All edge cases covered. No defects found. Ready for merge.
